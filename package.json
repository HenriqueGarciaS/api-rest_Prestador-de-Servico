{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "git pull && npx sequelize db:migrate && nodemon src/server.js",
    "start": "node src/server.js",
    "build": " git pull && npx sequelize db:create && nodemon src/server.js",
    "newBuild" : "git pull && npx sequelize db:migrate:undo:all && npx sequelize db:migrate && nodemon src/server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "multer": "^1.4.2",
    "mysql2": "^1.5.2",
    "sequelize": "^5.22.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.4",
    "sequelize-cli": "^5.5.1"
  }
}
